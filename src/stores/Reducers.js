// import createReducer from './ReducerFactory';

// const projectsReducer = createReducer({
//     projects: new List(),
//     getProjectById(id) {
//         return this.projects ? this.projects.find(it => it.id === id) : null;
//     },
// });

// export default {projectsReducer};
